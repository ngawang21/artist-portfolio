---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

// Get featured artworks from content collection
const artworksEntries = await getCollection('artworks', ({ data }) => data.featured === true);
const featuredArtworks = artworksEntries.slice(0, 3).map(entry => ({
  id: entry.id,
  slug: entry.id,
  title: {
    en: entry.data.title_en,
    fr: entry.data.title_fr
  },
  description: {
    en: entry.data.description_en,
    fr: entry.data.description_fr
  },
  image: entry.data.image,
  dimensions: entry.data.dimensions,
  price: entry.data.price,
  year: entry.data.year,
  medium: entry.data.medium,
  sold: entry.data.sold,
  featured: entry.data.featured,
  mollieLink: entry.data.mollie_link
}));
---

<BaseLayout title="Kunga DEMPA TSANG - Artist Portfolio">
  <!-- Hero Section -->
  <section class="relative h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 to-gray-100">
    <div class="container mx-auto px-4 text-center">
      <h1 class="font-serif text-6xl md:text-8xl font-bold mb-6 text-gray-900">
        Kunga DEMPA TSANG
      </h1>
      <p class="text-xl md:text-2xl text-gray-600 mb-8 max-w-2xl mx-auto">
        Art Contemporain de Paris
      </p>
      <div class="flex gap-4 justify-center">
        <a 
          href="/gallery/" 
          class="px-8 py-3 bg-primary text-white rounded-md hover:bg-primary/90 transition-colors text-lg font-medium"
        >
          View Gallery
        </a>
        <a 
          href="/about/" 
          class="px-8 py-3 border-2 border-primary text-primary rounded-md hover:bg-primary hover:text-white transition-colors text-lg font-medium"
        >
          About the Artist
        </a>
      </div>
    </div>
  </section>

  <!-- Featured Artworks -->
  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <h2 class="font-serif text-4xl md:text-5xl font-bold text-center mb-4">
        Featured Artworks
      </h2>
      <p class="text-center text-gray-600 text-lg mb-16 max-w-2xl mx-auto">
        Discover a selection of my latest works exploring liminal spaces and contemporary themes.
      </p>
      
      <div class="grid md:grid-cols-3 gap-8">
        {featuredArtworks.map((artwork) => (
          <div class="group bg-white rounded-lg overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300">
            <div class="aspect-[4/3] overflow-hidden">
              <img 
                src={artwork.image} 
                alt={artwork.title.en}
                class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
                loading="lazy"
              />
            </div>
            <div class="p-6">
              <h3 class="font-serif text-2xl font-semibold mb-2">{artwork.title.en}</h3>
              <p class="text-gray-600 mb-4 line-clamp-2">{artwork.description.en}</p>
              <div class="flex items-center justify-between">
                <span class="text-xl font-bold text-primary">€{artwork.price.toLocaleString()}</span>
                <a 
                  href="/gallery/" 
                  class="text-primary hover:underline font-medium"
                >
                  View Details →
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      <div class="text-center mt-12">
        <a 
          href="/gallery/" 
          class="inline-block px-8 py-3 bg-primary text-white rounded-md hover:bg-primary/90 transition-colors text-lg font-medium"
        >
          View Full Gallery
        </a>
      </div>
    </div>
  </section>

  <!-- About Preview -->
  <section class="py-20 bg-gray-50">
    <div class="container mx-auto px-4">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <img 
            src="https://images.unsplash.com/photo-1513364776144-60967b0f800f?w=800&q=80" 
            alt="Artist at work"
            class="rounded-lg shadow-lg"
            loading="lazy"
          />
        </div>
        <div>
          <h2 class="font-serif text-4xl md:text-5xl font-bold mb-6">
            About the Artist
          </h2>
          <p class="text-gray-600 text-lg mb-6 leading-relaxed">
            Dans mes tableaux, ce que j'ai cherché à peindre, ce sont ces moments suspendus du quotidien, quand la solitude et la contemplation se mêlent.
          </p>
          <p class="text-gray-600 text-lg mb-8 leading-relaxed">
            Je me suis inspirée des espaces liminaux, ces lieux entre le réel et l'imaginaire. J'ai essayé de les traduis dans mes peintures à travers la lumière, les textures et les superpositions.
          </p>
          <a 
            href="/about/" 
            class="inline-block px-8 py-3 bg-primary text-white rounded-md hover:bg-primary/90 transition-colors text-lg font-medium"
          >
            Learn More
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
